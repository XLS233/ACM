#include <bits/stdc++.h>

#define int long long

#include "mytemp/acm.h"

using namespace std;

using i64 = long long;

signed main() {
#ifndef QREAD
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
#endif

    return 0;
}